// src/types/unitTypes.ts

export interface Unit {
  id: string;
  title: string;
  description: string;
  // Agrega otras propiedades relevantes para una unidad, como actividades asociadas, etc.
}

// Puedes agregar otros tipos relacionados con unidades aquí
